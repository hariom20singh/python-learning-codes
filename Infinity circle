import turtle
wn = turtle.Screen()
alex = turtle.Turtle()

alex.speed(0)
alex.hideturtle()

while (1<2):
    for clr in ['violet', 'indigo', 'blue', 'green', 'yellow', 'orange', 'red']:
        alex.color(clr)
        alex.forward(100)
        alex.left(98)
